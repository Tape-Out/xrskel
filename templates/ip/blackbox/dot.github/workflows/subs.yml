name: subs

on:
  schedule:
    # 每月一次：上游往前走了子模块不会自己跟，不跟不会报错，只会越来越旧
    - cron: "23 4 3 * *"
  workflow_dispatch:

jobs:
  subs:
    uses: Tape-Out/.github/.github/workflows/subs.yml@main
    permissions:
      contents: write
      pull-requests: write
